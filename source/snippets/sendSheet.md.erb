> Example Request:

```shell
curl https://api.smartsheet.com/2.0/sheets/{sheetId}/emails \
-H "Authorization: Bearer ACCESS_TOKEN" \
-H "Content-Type: application/json" \
-X POST \
-d '{"sendTo" : [{"email": "john.doe@smartsheet.com"}, {"groupId": 2258118617917316}], "subject": "Check these rows out!", "message": "Here are the rows I mentioned in our meeting", "ccMe": false, "format": "PDF", formatDetails": {"paperSize": "A4"}}'
 
```

```csharp
Recipient[] recipients = new Recipient[] { new Recipient { Email = "john.doe@smartsheet.com" }, new Recipient { GroupId = 2258118617917316 } };
SheetEmail sheetEmail = new SheetEmail { SendTo = recipients, Subject = "Check this report out!", Message = "Here are the rows I mentioned in our meeting", CcMe = false, Format = SheetEmailFormat.PDF, FormatDetails = new FormatDetails { PaperSize = PaperSize.A4 } };

smartsheet.SheetResources().SendSheet(sheetId, sheetEmail);
```

> Example Response:

```json
{
    "message": "SUCCESS",
    "resultCode": 0
}
```

`POST /sheets/{sheetId}/emails`

Sends the Sheet as a PDF attachment via email to the designated recipients.

     |     |
-----|-----|
<%= SCOPE %> | SHARE_SHEETS
<%= HEADERS %> | <%= HEADER_AUTH %><br/><%= HEADER_CT_JSON %>
<%= REQUEST %> | [SheetEmail object](#sheetemail-object)
<%= RETURNS %> | [Result object](#result-object)
Figure out where this belongs. Make into a real snippet.

*Accept*       | Optional; if not specified, `*/*` is assumed (that is, any response type is accepted), else it must match the output of the invoked endpoint (typically `application/json`).
